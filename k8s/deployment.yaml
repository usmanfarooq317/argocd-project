apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app
  namespace: gitops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-app
        image: usmanf/python-argocd-app:latest
        ports:
        - containerPort: 8000
